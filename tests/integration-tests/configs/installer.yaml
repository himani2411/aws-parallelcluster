{%- import 'common.jinja2' as common with context -%}
---
test-suites:
  dcv:
    test_dcv.py::test_dcv_configuration:
      dimensions:
        - regions: [ "use1-az5" ]
          instances: ['p5.48xlarge']
          oss: [ "alinux2" ]
          schedulers: [ "slurm" ]
